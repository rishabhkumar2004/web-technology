html list:
ordered<ol></ol>
unordered<ul></ul>
description  list :
<dl>
  <dt>Coffee</dt>
  <dd>Black hot drink</dd>
  <dt>Milk</dt>
  <dd>White cold drink</dd>
</dl>






-->ordered list (attributes):
                   -> compact (The compact attribute specifies that the list should render smaller than normal, by reducing the space between lines and the indentation of the list.)
                   ->reversed (order will be in decending form )
                   ->start (from where to start )
                   ->type(the kind of numbering you want )






--> tables in html:

                -> <table>- initialization of table 
                -></table>- closing of a table tag 
                ->The <tbody> tag is used to group the body content in an HTML table.
                ->The <td> tag defines a standard data cell in an HTML table.
                ->The <tr> tag defines a row in an HTML table.
                ->The <th> tag defines a header cell in an HTML table.





--> <table border="1">: use to provide a definative border ratio.